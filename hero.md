LABEL: TOP STORY
# HOWL STREET goes live with auto-updating market data

This is the starter hero. Edit `hero.md` in the repo to change it — the next workflow run will pick it up. Use `*asterisks*` for *emphasis*. Delete the file (or leave it empty) to hide this section entirely.
